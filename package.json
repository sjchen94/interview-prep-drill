{
  "name": "interview-prep-drill",
  "version": "0.1.0",
  "private": true,
  "description": "Personal interview-prep drill — OpenClaw subagent that owns #drill (Blind 75 + sysdesign flashcards) using SM-2 scheduling.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx scripts/smoke.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
